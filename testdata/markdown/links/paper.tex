\section{A LaTeX doc}

This file exists so the static-mode link demo can show what happens
when an in-tree non-markdown file is linked: the toast says "not
available in static mode (run `mdp watch`)" and the page does not
change.
